telegram:
    resource: "@TelegramBundle/Controller/"
    type:     annotation
    prefix:   /telegram

app:
    resource: "@AppBundle/Controller/"
    type:     annotation

fos_user:
    resource: "@FOSUserBundle/Resources/config/routing/all.xml"
