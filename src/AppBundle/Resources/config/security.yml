services:
  app.security.wallet_voter:
    class: AppBundle\Security\WalletVoter
    tags:
      - { name: security.voter }
    public: false

  app.security.transaction_voter:
    class: AppBundle\Security\TransactionVoter
    tags:
      - { name: security.voter }
    public: false
